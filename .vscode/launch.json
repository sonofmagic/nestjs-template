{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "pnpm start:dev",
      "name": "Server Debug",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}/apps/server"
    }
  ]
}
